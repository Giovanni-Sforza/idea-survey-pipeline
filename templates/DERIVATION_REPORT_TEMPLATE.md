# Analytic Derivation Report: {derivation_title}

> Cross-paper symbolic derivation of `{target_observable_short}` as a
> function of `{control_parameter}`.
>
> Generated by `/skill:analytic-derivation`.

---

## ARIS_GUIDANCE_START

<!-- Iterative refinement block. Add your notes/corrections BEFORE re-running
     the skill. The skill reads this block at Step 0 to know what to
     prioritize on the next pass. -->

### What to do differently next run
- (empty)

### Steps the human disagrees with
- (empty)

### Equations to remove from the chain
- (empty)

### Equations to add to the chain
- (empty)

## ARIS_GUIDANCE_END

---

## 0. TL;DR

| | |
|---|---|
| **Target observable** | `{target_observable_latex}` |
| **Final analytic form** | `{final_answer_latex}` |
| **Validity regime** | {validity_regime_summary} |
| **Acceptance criteria passed** | {N_passed} / {N_total} |
| **Items still needing human verification** | {N_needfix} |
| **Wall-clock used** | {minutes} min |

**Final answer (one line):**
$$
{final_answer_latex}
$$

**Headline interpretation (1–2 sentences):**
{e.g. "The longitudinal Lambda polarization variance is, to leading order,
quadratic in the nuclear deformation parameter beta_2, with a coefficient
that factorizes into the shockwave proportionality C_rho^2, the spin-vorticity
coupling kappa_pol^2, and the event-by-event fluctuation width
sigma_beta_2^2 of beta_2 itself."}

---

## 1. User-supplied target spec (echo)

Verbatim copy of `derivation-target.md` at the time of this run.

> {echo of derivation-target.md sections 1, 3, 4, 5, 6}

---

## 2. Per-paper contributions

One subsection per anchor paper. Each subsection is a condensation of the
corresponding `cards/paper_*.json`.

### 2.{n} {first_author} et al. ({year}) — arXiv:{arxiv_id}

- **Expected role in chain:** `{role}` (confidence: {confidence})
- **What chain segment it covers:** {one_sentence}
- **Candidate equations used in the derivation:**

| Eq ID | LaTeX (verbatim from paper) | Inputs → Outputs | Validity regime |
|---|---|---|---|
| {paper_eqN} | `{latex}` | {inputs} → {outputs} | {regime} |

- **Stated assumptions inherited into our ladder:**
  - {assumption_1}
  - {assumption_2}
- **Numerical anchors used in Step 6 magnitude check:**
  - {anchor_1}

---

## 3. Chain sketch

A DAG of the derivation, from the user's free variable to the target
observable, with each edge tagged to the contributing paper.

```
beta_2  ── A1 (user) ──▶  rho_2  ── Paper 1, eq3 ──▶  omega_z
                                                       │
                                                       │ Paper 2, eq7
                                                       ▼
                                                      P_z
                                                       │
                                                       │ Step 5 ⟨·⟩_gaussian
                                                       ▼
                                              ⟨P_z²⟩ − ⟨P_z⟩²
```

**User confirmation:** approved at Checkpoint #1 on {timestamp}.
{If edited, list the edits.}

---

## 4. Unified notation table

| Unified symbol | Meaning | Dimensions | Paper 1 local | Paper 2 local | Paper 3 local | Paper 4 local | Clashes |
|---|---|---|---|---|---|---|---|
| `beta_2` | nuclear quadrupole deformation | 1 | `\beta_2` | `\beta_2` | `\beta^{(2)}` | `\beta_2` | none |
| `rho_2` | shockwave quadrupole modulation | 1 | `\rho_2` | `\rho^{(2)}` | — | — | **paper 3 uses `\rho_2` for matter 2-point function — DO NOT confuse** |

---

## 5. Assumption ladder

The ladder is built in three tiers. Each item is tagged with a status from
Step 4b literature verification: `user_axiom` / `confirmed` / `partial` /
`not_found` / `disputed`.

### Tier 1 — User axioms (taken as given; no verification attempted)
- **A1** [`user_axiom`] {assumption}
- **A2** [`user_axiom`] {assumption}

### Tier 2 — Paper-stated assumptions (verified against the source paper)
- **P1** [`confirmed`] {assumption} — *Source: paper 1 §2.*
- **P2** [`confirmed`] {assumption} — *Source: paper 2 §3 above Eq.(7).*

### Tier 3 — Derivation-implied assumptions (surfaced by `theory-synthesizer`, verified in Step 4b)
- **D1** [`confirmed`] {assumption} — *Cross-checked against [{other paper}].*
- **D2** [`partial`] {assumption} — *Only one independent confirmation found; coefficient may differ by O(1).*
- **D3** [`not_found`] {assumption} — *No independent literature found; treated as a NEEDFIX (see §9).*

---

## 6. Derivation steps

Each step is a verbatim copy of one entry in `derivation_trace.json`. The
SymPy `result` is shown; the `comment` from the script is shown verbatim.

### Step 1 — `{op}`
**Operation:** {op} (target=`{into}`, save_as=`{save_as}`)
**Comment:** {step.comment}
**Result:**
$$
{result.latex}
$$
**Assumptions invoked:** {A1, P1}

### Step 2 — `{op}`
... (repeat per step)

### Final equation
$$
{final_eq.latex}
$$

---

## 7. Verification report

From the `hep-theory-reviewer` subagent (Step 6). Verbatim summary; the full
report is at `verification_report.md`.

| Check | Status | Notes |
|---|---|---|
| Dimensional consistency of final equation | ✓ / ✗ | {one line} |
| Limit `{var} -> {limit}` gives expected `{expected}` | ✓ / ✗ | {one line} |
| Magnitude estimate at `{anchor_point}` | ✓ / ✗ | {one line} |
| Parity / time-reversal behavior | ✓ / ✗ | {one line} |
| User acceptance criterion #1 | ✓ / ✗ | {one line} |

**Reviewer overall verdict:** ready / almost / not ready
**Reviewer score:** X / 10

---

## 8. Literature verification summary (Step 4b)

| Item ID | Claim (short) | Status | Independent evidence |
|---|---|---|---|
| `verify_rho2_beta2_proportionality` | rho_2 = c_rho * beta_2 leading order | `confirmed` | [Smith et al. 2019], [Wang et al. 2022] |
| `verify_omega_z_from_rho2` | omega_z ~ rho_2 / tau_fb | `partial` | [Becattini & Karpenko 2017] — same form, different overall factor |
| `verify_gaussian_beta2_distribution` | beta_2 fluctuates Gaussian about its mean | `not_found` | (No independent paper found stating this distributional form for nuclear deformation; flagged as NEEDFIX) |

---

## 9. Human verification checklist

The following items the AI is NOT confident enough to certify. A human (the
user, the advisor, or a graduate student doing the algebra by hand) should
spot-check each before this derivation is treated as a result.

- [ ] **{Step N}** — {what to check, why it's uncertain, what success would look like}
- [ ] **{Tier-3 assumption D3}** — independent literature search returned
      nothing; either find a primary source or weaken the derivation to not
      depend on this assumption.
- [ ] **{Magnitude estimate}** — the derived coefficient lands at
      {numerical_value}, vs. the STAR measurement at {measured_value};
      factor-of-{X} discrepancy may indicate a missing dimensional factor
      or a wrong assumed scale.

---

## 10. Suggested next actions

(USER-FACING. The skill does NOT auto-invoke any of these — copy and run
manually.)

- If the chain looks RIGHT but the final coefficient is uncertain:
  `/skill:research-debug "Is there a published independent calculation of the coefficient ... ?" — depth: L2`
- If a tier-3 assumption is shaky:
  `/skill:research-lit "<assumption topic>" — deep analyze: true, max: 3`
- If the derivation chain itself feels wrong:
  edit `derivation-target.md` (section 4 physical-picture sketch), then re-run:
  `/skill:analytic-derivation "{topic}"`
- If the result is solid and you want a write-up:
  copy this report into a `proposal/` workspace and run `/skill:research-proposal`.

---

## 11. Resume / re-run hints

- Cards: `cards/paper_*.json` are CACHED. Delete a single card to force its
  paper to be re-extracted; delete the whole `cards/` directory to redo
  Step 2.
- Notation table and assumption ladder: re-run Step 3 / Step 4 by deleting
  `notation_table.md` / `assumption_ladder.md`.
- SymPy derivation: edit `derivation_script.json` by hand and re-run:
  ```bash
  python3 tools/symbolic_derive.py run \
      --script analytic-derivation/<run_slug>/derivation_script.json \
      --output analytic-derivation/<run_slug>/derivation_trace.json
  ```
  Then re-invoke the skill — it will skip Steps 1–4 and go straight to
  re-rendering `derivation_steps.md` from the new trace.

---

*Generated by `/skill:analytic-derivation`. Trace file:
`analytic-derivation/<run_slug>/derivation_trace.json` (SymPy
auditable).*
